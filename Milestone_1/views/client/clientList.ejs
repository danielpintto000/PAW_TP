<!DOCTYPE html>
<html>

<head>
  <!--Título do separador-->
  <title>Clients</title>

  <!--Usar o CSS definido em public/stylesheets/listStyle.css-->
  <link rel='stylesheet' href='/stylesheets/listStyle.css' />
</head>

<body>
  <!--Criar uma divisão da classe container definida no CSS (stlesheets/detailStyle.css)-->
  <div class="container">

    <!--Título da lista-->
    <h1>Clients List</h1>

    <!--Se existirem clientes-->
    <% if(items.length>0) { %>

      <!--Criar uma tabela da classe table_details definida no CSS (stlesheets/listStyle.css)-->
      <table class="table_details">
        <thead>
          <tr>
            <th hidden>#</th>
            <th hidden>Id</th>
            <th>Client Name</th>
            <th>Email</th>
            <th>Address</th>
            <th></th>
            <th hidden>Details</th>
            <th hidden>Edit</th>
            <th hidden>Delete</th>
          </tr>
        </thead>
        <tbody>

          <% for(var i=0; i<items.length;i++) { %>
            <tr>

              <td hidden>
                <%=i%>
              </td>
              <td hidden>
                <%=items[i]._id%></a>
              </td>
              <td>
                <%= items[i].name%></a>
              </td>
              <td>
                <%= items[i].email%></a>
              </td>
              <td>
                <%= items[i].address%></a>
              </td>

              <!--Apresentar a opção Show Details que irá apresentar detalhes de um cliente (client)-->
              <td><a class="showDetail" href="/clients/show/<%= items[i]._id%> "> Show Details </td>    
            </tr>

            <% } %>
        </tbody>
      </table>

      <!--Se não existirem clientes-->
      <% } else { %>
        <div>No clients found.</div>
        <% } %>
  </div>

  <div>
    <!--Apresentar o butão Back que fará voltar atrás-->
    <button><a href="/"> Back </a></button>
  </div>
  
</body>

</html>